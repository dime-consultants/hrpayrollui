version: "3.9"

services:

  frontend:
    build: .
    container_name: web-ui
    restart: unless-stopped
    ports:
      - "80:80"
    networks:
      - frontend_network

networks:
  frontend_network:
    driver: bridge
