version: "3.9"

services:

  frontend:
    build: .
    container_name: hr_frontend
    restart: unless-stopped
    ports:
      - "80:80"
    networks:
      - frontend_network

networks:
  frontend_network:
    driver: bridge
